using MazeGenerators;
using System;
using System.Collections;
using System.Collections.Generic;
using System.Threading.Tasks;
using UnityEngine;

namespace Assets.Scripts.Managers
{
    public class PlayerManager : MonoBehaviour
    {
        


        //async void MovePlayerAsync(Vector3Int direction)
        //{
        //    coordInMaze = await Task.Run(() => MovePlayer(direction));
        //}

        //Vector2Int MovePlayer(Vector3Int direction)
        //{
        //    var newCoords = new Vector2Int();
        //    Player.transform.position = Vector3.Lerp(Player.transform.position, direction, Time.deltaTime);
        //    return  newCoords;
        //}
    }
}